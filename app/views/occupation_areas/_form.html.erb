<%= form_with(model: occupation_area) do |form| %>
  <% if occupation_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(occupation_area.errors.count, "error") %> prohibited this occupation_area from being saved:</h2>

      <ul>
        <% occupation_area.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
