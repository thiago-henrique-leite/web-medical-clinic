<div class="container">
  <div id="logreg-forms">
    <div class="row justify-content-center">
      <% if anamnesis_consultation.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(anamnesis_consultation.errors.count, "error") %> prohibited this anamnesis_consultation from being saved:</h2>

          <ul>
            <% anamnesis_consultation.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>
      <%= form_with(model: anamnesis_consultation) do |f| %>
        <h1 class="h3 mb-3 font-weight-normal" style="text-align: center">Anamnese - Consulta <%=params[:consultation_id]%></h1>
        <%= f.hidden_field :anamnesi_id, value: 1 %>
        <%= f.hidden_field :patient_id, value: current_patient.id %>
        <%= f.hidden_field :consultation_id, value: params[:consultation_id] %>

        <%= f.label Anamnesi.find(1).questions[0].description %>
        <%= f.text_field :answer, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[1].description %>
        <%= f.text_field :answer1, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[2].description %>
        <%= f.text_field :answer2, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[3].description %>
        <%= f.text_field :answer3, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[4].description %>
        <%= f.text_field :answer4, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[5].description %>
        <%= f.text_field :answer5, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[6].description %>
        <%= f.text_field :answer6, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[7].description %>
        <%= f.text_field :answer7, class: "form-control shadow rounded" %>
        <%= f.label Anamnesi.find(1).questions[8].description %>
        <%= f.text_field :answer8, class: "form-control shadow rounded" %>
        <hr>

        <div class="form-group row justify-content-center">
            <%= f.submit "Responder", class: "btn btn-outline-light btn-lg signup" %>
            <style> 
              .btn.btn-outline-light.btn-lg.signup {
                  background-color: green;
                  transition: all 0.3s;
                  cursor: pointer;
                  align-items: center;
              }
              .btn.btn-outline-light.btn-lg.signup:hover{
                -webkit-filter: drop-shadow(7px 5px 3px rgba(0,0,0,.5));
                filter: drop-shadow(7px 5px 3px rgba(0,0,0,.5));
              }
            </style>  
          </div>

          <div class="form-group row justify-content-center">
            <%= link_to "Voltar", "/dashboard/patient#consults", class: "btn btn-outline-light btn-lg back" %><br/> 
            <style> 
              .btn.btn-outline-light.btn-lg.back {
                  background-color: red;
                  transition: all 0.3s;
                  cursor: pointer;
                  align-items: center;
              }
              .btn.btn-outline-light.btn-lg.back:hover{
                -webkit-filter: drop-shadow(7px 5px 3px rgba(0,0,0,.5));
                filter: drop-shadow(7px 5px 3px rgba(0,0,0,.5));
              }
            </style>  
          </div>
      <% end %>
    </div>
  </div>
</div>



